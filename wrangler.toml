name = "domaincenter"
main = "worker.js"
compatibility_date = "2024-01-01"

# Workers 환경 변수 (민감한 정보는 wrangler secret 명령어로 설정)
[vars]
BASE_DOMAIN = "jiwungum.dpdns.org"

# KV 네임스페이스 (선택사항 - 도메인 정보 저장용)
# [[kv_namespaces]]
# binding = "DOMAINS"
# id = "your_kv_namespace_id"

# 라우트 설정 (필요시)
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
